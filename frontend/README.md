# Frontend (clean v1)

Next.js operator UI at `/ops`:

- `/ops/cases`
- `/ops/cases/:id`
- `/ops/approvals`
- `/ops/operations`
- `/ops/evals`

## Run

```bash
npm install
npm run dev
```

Dev server runs on `http://localhost:3001`.
